# CoCoder Frontend

Vite + React + TypeScript dashboard for CoCoder runs, repos, and live pipeline events.

## Dev

```bash
cd frontend
npm install
npm run dev
```

API defaults to `http://localhost:8000`. Override with `VITE_API_BASE`.

Start the API:

```bash
cd server
uv run python main.py
```
